import React, { useContext } from 'react';
import { UserContext } from './viewCart.jsx';

export default function PayedBooks(props) {
    const user = useContext(UserContext);
    return (
	<>
	    
	</>
    )
}
